# WYEA 사이트 — Google Ad Grants 승인 작업 현황

이 문서는 다른 AI/세션이 이어받아 작업할 수 있도록 지금까지의 진행 상황을 정리한 것입니다. (최종 업데이트: 2026-09-17, Codex)

## 새 세션에서 먼저 읽기

1. 이 문서와 `git status --short`를 확인한 뒤, 수정할 파일의 실제 코드를 읽는다. 문서와 코드가 다르면 차이를 확인하고 문서를 갱신한다.
2. 사용자가 원하는 것은 기존 디자인과 GitHub Pages 구조를 유지하면서 콘텐츠와 기술을 보강하는 것이다. 전체 재설계는 하지 않는다.
3. 작업이 끝나면 이 문서의 현재 상태·다음 작업·검증 결과를 갱신한다. 중요한 결정은 아래 변경 기록에 날짜와 함께 남긴다. 문서 갱신을 위해 별도 허락을 묻지 않는다.
4. 다른 AI나 사용자의 미커밋 변경을 되돌리지 않는다. 빌드 통과와 실제 배포/브라우저 확인은 구분해서 기록한다.

## 확인된 단체 정보

- 현재 사이트에 사용할 단체명: **세계청년교류연합**, 약어 **WYEA** (2026-09-17 사용자 정정). 이전 대화의 고유번호증 기재명은 세계청년교류회였으나, 증서 기재명 자체도 정정됐는지는 별도 확인되지 않았다. 사이트 표시명은 최신 요청을 따른다.
- 고유번호: **410-82-93357**. 사용자가 고유번호증 발급 사실을 확인했다.
- 고유번호증만을 근거로 **비영리법인**이라고 단정하지 않는다. 현재 소개는 비영리단체로 표현한다.
- 홈페이지: https://wyea.info/ (GitHub Pages + 자체 도메인).

## 목표

세계청년교류연합(WYEA) 비영리단체 사이트를 개편해서 **Google Ad Grants 승인**을 받는 것. 심사에서는 실제 존재하는 단체인지, 콘텐츠가 진짜인지(추정/허위 없음), 사이트가 정상 작동하는지를 봅니다.

2026-09-17: 사용자 정정에 따라 홈·단체소개·활동 설명·푸터·개인정보 처리방침·페이지 제목 및 설명·프리렌더 메타데이터의 단체명을 세계청년교류연합으로 통일했다. 조사도 함께 수정했다. 기존 고유번호와 영문명은 유지했다. 텍스트 수정으로 별도 빌드/배포 검증은 하지 않았다.

## 원래 세운 5단계 계획

1. **사실 확인**: 공식 명칭·비영리 지위·학교명·운영 중인 사업·공개할 실제 활동 자료 확정
2. **기술 보완**: 하위 주소 응답, 앱 초기화 실패, 모집 버튼, 재정보고 상태와 개인정보 문구 정리
3. **핵심 콘텐츠**: 단체소개·주요사업·참여/문의·실제 활동 기록 작성
4. **화면 연결**: 기존 카드와 폴라로이드에서 상세 페이지로 이동하도록 메뉴·링크 추가
5. **배포 후 검증**: 직접 주소 접속, 모바일, 링크, HTTPS, 로딩, 공개 내용 확인 후 재심사 요청

## 현재 상태

### 1. 사실 확인 — 일부 완료, 추가 활동 정보 대기
- 확인된 실제 활동 1건 등록됨: 재일본대한민국청년회와의 서울 교류회 (2025-11-01~03). `src/content/activities.ts`

2026-09-17: 사용자가 서울 교류회 일정을 11월 1~3일로 정정했다. 공통 활동 데이터의 날짜·종료일·설명과 상세 화면 날짜를 수정했다. 단체소개 연혁은 같은 데이터를 참조하므로 함께 반영된다. 텍스트 수정으로 별도 빌드/배포 확인은 수행하지 않았다.
- 홈 화면 연혁([HomeView.vue](src/views/HomeView.vue))과 About 페이지 연혁([AboutView.vue](src/views/AboutView.vue))의 협력 단체명이 "재일한국청년동맹" → "재일본대한민국청년회"로 정정됨 (2025-07-14 협력 체결 항목).

### 2. 기술 보완 — 대부분 완료, 참고사항 1건
- `npm run build` (= `vite build` + `node scripts/build-about.mjs`) 성공 확인됨. `/about`, `/activities`, `/activities/2025-seoul-exchange` 3개 경로가 정적 HTML로 프리렌더되어 직접 주소 접속 시 실제 콘텐츠가 응답함.
- `.github/workflows/pages.yml`의 `dist/index.html → dist/404.html` 복사로 다른 SPA 경로에서도 화면 복구가 가능하다. 다만 GitHub Pages의 404 폴백은 HTTP 404 상태를 그대로 반환할 수 있으므로 정상 200 응답과 같지 않다. 공개 상세 페이지는 정적 HTML 생성 대상에 포함하고 배포 후 응답을 확인해야 한다.
- 모집(가입) 버튼: `HomeView.vue`의 "가입하러 가기" 버튼은 현재 `handleJoinClick`에서 "지금은 가입 시즌이 아닙니다" alert만 띄우도록 되어 있음 (의도된 상태로 보임).
- 재정보고: `FinancialReport.vue`에 2025년 실제 수입/지출 데이터 있음, 2026년은 빈 배열(진행 중).
- 개인정보 처리방침: `PersonalInformationProcessingPolicy.vue`에 기본 항목 작성돼 있음.
- **참고(수정 보류)**: `src/main.js`의 `bootstrap()`이 로그인 상태일 때 `await userStore.getMyInfo()`를 catch 없이 기다린 뒤 `app.mount()`하는 구조라, 토큰이 유효하지 않은 상태에서 백엔드 호출이 실패하면 앱이 마운트되지 않고 흰 화면이 될 수 있음. **이 부분은 사용자가 명시적으로 "신경 쓰지 말라"고 함** — 원래 로그인 기능을 포함한 동적 사이트로 만들려다 시간 부족으로 중단했고, 로그인/인증 관련 코드(`/login`, `/register`, `/user/*` 라우트, `stores/user-store.ts`, `utils/token-utils`)는 미완성 상태로 남겨둔 것. 버그로 지적하거나 고치지 말 것.

### 3. 핵심 콘텐츠 — 진행 중
- 단체소개(`AboutView.vue`): 목적, 설립 배경, 연혁, 단체 기본정보, 문의 완료.
- 활동소식(`ActivitiesView.vue` + `src/content/activities.ts`): 서울 교류회, 2026-03-04 삼원가정봉사원파견센터 봉사활동, 2026-04-18 도쿄 피크닉, 2026-04-21 오사카지부 교류회 총 4건. 최신순 목록과 활동별 상세 페이지를 제공한다.
- 2026-09-17: 사용자 요청으로 삼원가정봉사원파견센터 봉사활동을 추가했다. 상세 경로는 `/activities/2026-samwon-volunteering`. 설명은 확인된 기관·날짜·봉사활동 사실만 포함하며 사진은 미지정으로 표시하지 않는다. 봉사활동에 맞춰 `VOLUNTEERING` 및 ‘활동 기관’ 표시를 지원한다. `npm run build`, `npx vue-tsc --noEmit` 통과, 정적 상세 HTML 생성 확인. 배포/브라우저 확인은 하지 않았다.
- **대기 중**: 홈 폴라로이드 사진([HomeViewPhoto.vue](src/components/HomeViewPhoto.vue))에 나오는 아래 두 행사는 아직 activities.ts에 없음. 사용자가 같이 참석했던 사람들에게 정확한 날짜·장소·교류 단체명을 확인한 뒤 알려주기로 함 (AI가 임의로 추정해서 채우면 안 됨):
  - "2025년 상반기 정모"
  - "제2회 한일음악교류회"는 2026-09-17 사용자가 날짜를 2026-08-29로 확인하여 등록 완료. 장소·상대 단체·사진 지정은 아직 대기 중이다.

### 4. 화면 연결 — 미착수
- 홈 폴라로이드 사진들을 해당 활동 상세 페이지로 링크하는 작업은 아직 안 함. 위 3번 항목의 사실 확인이 끝나야 자연스럽게 진행 가능 (사진 ↔ activities.ts 항목 ↔ 상세 페이지 순으로 연결).
- `ActivitiesView.vue` 상세 페이지의 "단체 연혁 보기 →" 링크는 사용자 요청으로 제거됨 (이제 "← 활동소식 목록"만 있음).

### 5. 배포 후 검증 — 사용자 직접 진행
- **커밋/푸시, 로컬 확인(`npm run dev`)은 사용자가 직접 함.** AI는 매번 "커밋할까요?", "로컬에서 확인해볼까요?" 같은 질문을 하지 않아도 됨. 대신 `npm run build`나 `npx eslint`로 빌드/린트 검증까지는 AI가 해도 됨.

## 다음에 이어서 할 일 (우선순위)

1. **주요사업 설명 보강(다음 챕터 제안)**: 실제 운영 중인 사업과 향후 계획을 사용자에게 확인하고, 목적·대상·진행 방식·확인된 활동 사례를 정리한다. `/programs` 페이지와 상단 메뉴 연결을 제안하되, 아직 페이지 구현을 승인받거나 완료한 것은 아니다. 사례가 없는 사업의 실적을 만들어 쓰지 않는다.
2. **참여·문의 안내 보강**: 참여 대상, 모집 여부, 신청/문의 방법과 협력 문의 경로를 실제 운영 방식에 맞춰 안내한다. 모집 일정이나 자격을 임의로 정하지 않는다.
3. **실제 활동 기록 추가(정보 확인과 병행)**: 사용자가 정모/한일음악교류회의 사실을 확인하면 `src/content/activities.ts`와 목록/상세 조회 구조를 확장하고, 라우터(`src/router/index.js`) 및 `scripts/build-about.mjs`에 새 상세 경로를 추가한다.
4. **화면 연결**: 확인된 활동과 대응하는 홈 폴라로이드(`HomeViewPhoto.vue`) 사진에 상세 페이지 링크를 연결한다.
5. **검증 및 재심사 준비**: 변경 후 `npm run build`로 검증하고, 사용자가 배포 후 실제 URL/모바일/HTTPS/공개 콘텐츠를 확인한다. 주요사업·참여 안내 등 남은 콘텐츠 작업도 함께 점검한 뒤 재심사를 준비한다. 사진 연결만으로 준비 완료로 판단하지 않는다.

2026-09-16: 사용자 요청으로 문서 검토 의견 중 5번(다음 작업이 사진 연결에 치우친 문제)만 반영했다. 나머지 검토 의견에 해당하는 기존 항목은 이번 수정에서 변경하지 않았다.

## 작업 시 유의사항

- 활동소식은 **확인된 사실만** 기재. 사진, 세부 진행 내용, 날짜 등을 추정해서 채우지 않는다 (사용자 원칙).
- 로그인/인증 관련 코드는 미완성 기능으로 알고 있고 건드리지 않는다.

## 2026-09-16 Codex 변경 기록 및 현재 UI 결정

### 프리렌더 기술 수정
- `scripts/build-about.mjs`: Vite SSR 로딩을 production 환경으로 맞춰 프리렌더 HTML의 Vue scoped CSS 식별자와 배포 CSS가 일치하도록 수정했다.
- 생성 HTML의 scope 식별자가 연결된 CSS에 존재하는지 검증하며, 불일치하면 빌드를 실패시킨다. 이 설정/검증을 임의로 제거하지 않는다.
- 로컬 빌드와 세 페이지의 scope 일치를 확인했다. 실제 배포 확인을 했다는 뜻은 아니다.

### TypeScript 설정
- `tsconfig.json`: `rootDir: "./src"`를 명시했다. `baseUrl`을 제거하고 경로 별칭은 `"@/*": ["./src/*"]`로 유지했다.
- 사용자의 편집기에 TypeScript 6 관련 rootDir 오류가 표시됐고, 프로젝트 로컬 TypeScript는 당시 5.9.2였다. 수정 후 `npx vue-tsc --noEmit` 통과. 편집기 빨간 줄이 사라졌는지는 직접 확인하지 않았다.

### 활동소식 사진 테스트 및 목록 정리
- 2026-09-17 목록 디자인 보강: 목록 카드 전체를 RouterLink로 바꾸고 ‘국제교류/봉사활동’ 파란 배지와 장식 화살표 ↗를 추가했다. 카드 여백과 간격을 줄이고 hover/키보드 포커스 강조를 적용했다. 목록 사진·설명은 표시하지 않으며 상세 화면은 유지했다. `npm run build` 통과(기존 HomeViewPhoto 중복 import 경고 유지). 브라우저 시각 확인 및 배포는 하지 않았다. 이전의 제목만 클릭한다는 기록보다 이 변경을 우선한다.
- 파일: `src/views/ActivitiesView.vue`.
- 사용자 지정 `@/assets/picture/LINE_ALBUM_2日目_251209_16.jpg`를 임시 테스트 사진으로 import해 **상세 페이지에만** 표시한다. 원본 비율 유지, 최대 높이 560px, 최대 너비 100%, 가운데 정렬이다.
- 사진을 실제 행사 자료로 확정한 것은 아니다. 테스트 요청으로 넣었으므로 사진 내용/촬영일을 근거로 행사 사실을 추가하지 않는다.
- 목록은 날짜·장소·제목만 표시한다. 사용자 요청으로 썸네일과 별도 ‘활동 기록 보기’ 링크를 제거했고, 2026-09-17에는 목록의 설명도 제거했다. **제목 클릭으로 상세 페이지에 진입**하며 상세 설명은 유지한다. 이번 목록 설명 제거는 템플릿 한 줄 수정으로 별도 빌드/배포 검증은 하지 않았다.
- 상세 상단 표기는 `EXCHANGE`만 남겼다 (`· SEOUL` 제거). 행사 장소 정보 자체는 유지한다.
- 사진 추가 후 `npm run build` 통과, 썸네일 크기 조정 시 `npx vue-tsc --noEmit` 통과. 이후 썸네일/링크/문구 삭제는 작은 템플릿·스타일 수정으로 별도 빌드를 다시 실행하지 않았다. 브라우저에서의 시각 확인은 사용자가 진행한다.

### 연혁 항목 삭제
- 이후 사용자 요청으로 홈의 **설립 역사 섹션 전체**를 제거했다. `HomeView.vue`의 section3 마크업·전용 CSS·등장 IntersectionObserver를 삭제하고, 기존 하단 둥근 모서리와 그림자를 section2로 옮겼다. 이제 홈은 소개 → 설립 배경/주요 활동 카드 → 활동사진으로 이어진다. 고정 활동사진을 드러내는 section4 공간은 유지했다. **단체소개 페이지의 연혁은 유지한다.** 이 변경 후 `npm run build` 통과(기존 HomeViewPhoto 정적/동적 중복 import 경고는 남음). 브라우저 시각 확인/배포는 수행하지 않았다.
- 사용자 요청으로 `AboutView.vue`와 `HomeView.vue`의 연혁에서 **2025년 7월 2일 미국 유학생 네트워크와 협력 체결**, **2025년 7월 4일 경희대학교 지부 설립**을 삭제했다. 이후 작업에서 과거 기록을 근거로 복원하지 않는다.
- 데이터 항목과 해당 목록 요소만 삭제했다. 이번 수정에서는 별도 빌드나 배포 확인을 수행하지 않았다.

## 다음 작업 보충

- 2026-09-26 A 배포 확인(Codex, 후속 상태): 처리방침 정적 경로 보완을 `f8a9e1d`로 `feature/join`에 커밋·푸시한 뒤 `b2ea899`로 main에 병합·푸시했다. GitHub Pages 실행 `36164789963`의 build/deploy가 모두 성공했다. `https://wyea.info/`과 `/join/`, `/personalinformationprocessingpolicy/`는 HTTP 200이며, `/join/`은 가입 소개·절차 HTML과 canonical을, 처리방침은 집행부 필수 항목 문구와 canonical을 반환했다. 끝 슬래시 없는 처리방침 주소도 HTTP 200을 확인했다. 사용자 지시에 따라 브라우저 없이 HTTP·정적 HTML만 확인했으므로 실제 화면의 퀴즈 진행·양 트랙 입력·동의 화면은 이번 세션에서 확인하지 않았다. 이전 배포의 처리방침 HTTP 404 기록은 수정 전 상태다. 다음 작업 순서는 대표의 최신 지시대로 D → B → C다.

- 2026-09-26 A 공개 경로 보완(Codex): `feature/join`의 가입 코드와 작업 알림 코드를 `9465f50`으로 main에 병합·푸시했고 GitHub Pages 실행 `36164458106`의 build/deploy가 성공했다. 배포 후 `https://wyea.info/join/`은 HTTP 200으로 가입 소개 HTML을 반환했지만 처리방침 직접 주소 `/personalinformationprocessingpolicy`는 HTTP 404 폴백을 반환했다. 이에 `scripts/build-about.mjs`에 처리방침 정적 페이지와 CSS 연결을 추가하고 라우터 canonical에 끝 슬래시를 맞췄다. 수정 후 `vue-tsc --noEmit`, 라우터 ESLint, `npm run build`가 통과하고 `dist/personalinformationprocessingpolicy/index.html`이 생성됐다. 현재 기록 시점에는 이 보완의 재배포와 공개 경로 재확인은 아직이다. 기존 프리렌더 종료 시 `The build was canceled` 로그와 HomeViewPhoto 중복 import 경고는 남지만 명령 종료 코드는 0이다. 사용자 지시에 따라 브라우저 화면 조작은 하지 않고 HTTP·정적 HTML로 공개 경로를 확인한다.

- 2026-09-26 A 작업 알림 추가(Codex, 이번 세션): 대표가 Google Apps Script API 활성화와 `clasp`의 wyea@wyea.info 로그인을 완료했다고 이슈 #4에 댓글을 달았고, `clasp show-authorized-user`도 wyea 계정을 확인했다. 레포 밖 임시 폴더에서 `clasp pull`한 원격 `Code.js`와 레포 `apps-script/join/Code.gs`가 수정 전 바이트 단위로 같았다. 원격 manifest를 보존하고 `notify` 코드(고정 수신처 wyea@wyea.info, 코드 비밀값 확인, 제목 200자·본문 5000자 제한, 시트 미접근)를 추가했다. 레포에는 placeholder만 두고 실제 32자 난수는 레포 밖 `D:\10_Projects\Coding\WYEA\.notify.env`에 저장했다. 배포용 임시 사본에서만 비밀값을 치환해 `clasp push`했고 기존 웹 앱 배포 ID를 버전 4로 갱신했다. 모의 테스트 56개·mock-test.cjs ESLint 통과. 실연동 `notify` 시험 POST는 `{ok:true}`였고 대표가 메일 수신을 보고했다. 별도의 이슈 URL 알림 POST는 HTTP 404로 해당 요청의 발송 여부를 확인하지 못했으며, 그 뒤 `doGet`은 HTTP 200이었다. 신청자 개인정보는 작업 알림에 넣지 않았다. 아래 기록의 '푸시 전' 상태보다 이 기록과 후속 Git 상태를 우선한다.

- 2026-09-26 A 이메일 수정 및 푸시 재개(Codex, 최신 지시): 사용자가 `254413104+lchenter@users.noreply.github.com`을 지정하고 이메일 설정→기존 커밋 amend→인계서 별도 커밋→`feature/join` 푸시까지만 진행한 뒤 대기하도록 요청했다. 로컬 Git 이메일을 변경하고 `git commit --amend --reset-author --no-edit`로 `4ffe698`을 `4f715bf`로 갱신했다. 새 커밋의 작성자·커미터 이메일을 확인했으며 `git diff --exit-code 4ffe698 HEAD --`로 커밋된 파일 내용이 동일함을 확인했다. 이 문서는 푸시 전에 별도 커밋하는 기록으로, 실제 푸시 성공을 선기록하지 않는다. main 병합·배포·B/C/D는 사용자의 다음 지시까지 대기한다.

- 2026-09-26 A 최초 푸시 중단(Codex, 아래는 이메일 수정 전 이력): 사전 점검 5개를 통과한 뒤 `feature/join`에 `4ffe698`(`/join 회원 가입 신청 페이지 추가 및 조직도 개편 반영`, 지정한 10개 파일)을 커밋했다. 실제 `git push -u origin feature/join`은 `GH007: Your push would publish a private email address` / `push declined due to email privacy restrictions`로 거절됐다. dry-run과 GitHub 인증은 성공했지만 실제 커밋 이메일 공개 검사는 통과하지 못한 것이다. 당시 사용자 요청에 따라 재시도하지 않고 중단했으며, 작성자는 `lchenter` / `lch@lchenter.com`이었다. main 병합·Pages 배포·배포 후 화면 확인·B/C/D 착수는 하지 않았다.

- 2026-09-26 A 마무리 사전 점검(Codex, 이번 세션): 사용자가 `NEXT_LLM_HANDOFF.md` §5부터 진행하도록 요청했고, 추가 사전 점검 5개 통과 후 커밋·푸시·main 병합·배포 확인을 진행하도록 명시했다. 이번 작업에는 아래 과거의 사용자 직접 커밋·푸시 기록 대신 이 요청을 적용한다.
  - 수정 전 `feature/join` / `01d76af`에서 인계서의 미커밋 10개(수정 5·추가 5)만 존재함을 확인했다. `git push --dry-run origin feature/join` 성공, `gh auth status`의 활성 계정 `lchenter` 확인. 로컬 Git 작성자를 `lchenter` / `lch@lchenter.com`으로 설정했다.
  - 로컬 의존성이 없어 `npm ci`를 완료한 뒤 `npx vue-tsc --noEmit`, 인계서 §2의 5개 파일 ESLint, `npm run build`, 가입 서버 모의 테스트 50개가 모두 종료 코드 0으로 통과했다. `/join` 포함 8개 정적 페이지 생성 확인. 기존 HomeViewPhoto 중복 import 경고와 프리렌더 종료 시 `The build was canceled` 로그가 남았으나 빌드 명령 종료 코드는 0이다.
  - 처리방침에서 집행부의 역량·할 수 있는 일을 선택 항목으로 잘못 분류한 부분을 필수 항목으로 분리하고, 해 온 활동은 선택으로 유지했다. 확정된 가입 동의문·동의 버전·서버 검증은 그대로다. 문구 수정 후 해당 파일 ESLint와 빌드를 다시 통과했다.
  - 이 기록 작성 시점에는 커밋·푸시·Pages 배포·배포 후 브라우저 확인 전이다. 이전 세션의 Apps Script v3 실접수 확인은 인계 기록이며 이번 세션의 재검증이 아니다. 다음 순서는 A 커밋→푸시→main 병합→Pages 배포→실제 제출 없이 가입 동의 화면과 처리방침 확인이다. B·C·D는 A 배포 확인 후 진행한다.

- 2026-09-25 회원 가입 신청 페이지(/join) 추가, `feature/join` 브랜치. 실무자 온보딩(`/onboarding`)과 별개의 일반 회원 가입 흐름이다. 파일: `src/views/JoinView.vue`(소개 → 정보 입력 → 지원서 → 동의 → 완료), `src/content/join.ts`(선택지·글자 수·동의문·동의문 버전·API 주소), `apps-script/join/Code.gs`·`README.md`(서버 검증, 중복 휴대전화 거절, 만 14세 미만 거절, 숨김 칸·3초 제한 스팸 방지, 수식 이스케이프). `src/router/index.js`에 /join 라우트와 canonical, `scripts/build-about.mjs`에 /join 프리렌더와 JoinView CSS를 추가했다. 기존 파일은 삭제하지 않았고 홈 '가입하러 가기' 버튼은 아직 연결하지 않았다(출시 때 연결). `JOIN_API_URL`은 비어 있어 제출 버튼은 준비 중 안내를 띄운다. `npm run build`(프리렌더 포함), `npx vue-tsc --noEmit` 통과, Code.gs 모의 테스트 11개 통과, 로컬 preview에서 데스크톱·모바일 화면 확인. Apps Script 실제 배포·실연동·GitHub Pages 배포는 아직 하지 않았다.
- 2026-09-25 /join 흐름 변경(대표 요청): 소개(가입 절차) → '세계청년교류연합이 어떤 일을 하는 단체인지 알고 계신가요?'(알고 있어요 / 알려 주세요) → [알려 주세요] WYEA 소개(비전·가치관·하는 일·해 온 일=activities 목록·해 갈 일) → 확인 퀴즈 3문제(모두 맞혀야 통과, 틀린 문제만 다시 풀기, 정답 해설은 맞힌 문제에만 표시) → 지원서 → 정보 입력 → 동의 → 완료. 소개·퀴즈 문구는 `src/content/join.ts`의 `about`·`quiz`에 있다. 가치관·앞으로의 계획은 대표 확인 전 초안이다. `Code.gs`에 새 신청 알림 메일(`MailApp`, 스크립트 속성 `NOTIFY_EMAILS`, 기본 `wyea@wyea.info`, 개인 연락처·주소·생년월일 제외, 발송 실패해도 접수 성공)과 `testNotify`를 추가했다. 나중에 인사 담당 그룹 주소로 `NOTIFY_EMAILS`만 바꾸면 된다. build·vue-tsc 통과, 모의 테스트 12개(메일 발송·메일 실패 포함) 통과, preview 화면 확인.
- 2026-09-25 /join 지원서 변경(대표 요청): 가치관을 대표 문구(준비된 사람에게 기회가 찾아옵니다·완벽히 준비되지 않아도 됩니다 / 같이 성장합니다 / 수평적 문화를 위해 노력합니다 / 자율적으로 움직입니다)로 교체, 해 갈 일의 2027년 1월은 '도쿄 교류회'로만 표기. 지원서에 '활동하고 싶은 분야'(실무진 조직도 팀 중 1개 필수, 선택한 팀에 소속)를 추가. 일반 회원 지원서 상단에 실무진 지원 안내를 두고 누르면 같은 페이지의 `?track=staff`로 전환(퀴즈 통과·입력 내용 유지). 실무진 지원서는 분야·지원 동기·나의 역량·해 온 것(선택)·할 수 있는 것. 팀 목록 `teams`는 실무자방 분야 기준 임시안(행정·문서, 재무·회계, SNS·콘텐츠, 행사 기획, 대외협력, 지부 운영·모집)이며 조직도 확정 시 `join.ts`와 `Code.gs`의 `TEAMS`를 같이 바꾼다. 시트 헤더에 `track`·`team`·`competencies`·`experience`·`capabilities` 추가, 중복 확인 열을 헤더 이름으로 찾도록 변경. 모의 테스트 19개 통과.
- 2026-09-25 퀴즈 2번 해설의 청년회 협력 시점을 대표 확인에 따라 '2025년 6월'로 정정. 단, `src/views/AboutView.vue` 연혁에는 '2025년 7월 14일 재일본대한민국청년회와 협력 관계 체결'로 남아 있어 대표 확인 후 정리 필요(미수정). 모의 테스트를 `apps-script/join/mock-test.cjs`로 레포에 추가(실행: `node apps-script/join/mock-test.cjs`, 19개 통과, eslint 통과).
- 2026-09-26 /join 출시 준비(대표 요청으로 Claude가 진행, 커밋·병합은 대표):
  - 관심 활동 선택지를 실무진 조직도·해 온 활동 기준 14개로 교체하고 '아직 잘 모르겠음'과 단독 선택 로직 제거(`join.ts`·`JoinView.vue`·`Code.gs`). 개인정보 수집·이용 동의 항목에 '활동하고 싶은 분야'와 실무진 지원 항목을 추가(동의 버전은 아직 접수된 신청이 없어 2026-09-25 유지).
  - Apps Script 실제 배포 완료: 프로젝트 `WYEA 회원 가입 접수 (/join)`(wyea 계정), 시트는 공유 드라이브 `WYEA_LCE`의 `WYEA 회원 가입 신청(홈페이지)`. `setupSheet`·`testNotify` 실행 확인(알림 메일 wyea@wyea.info로 발송됨), 웹 앱 버전 1 배포, `JOIN_API_URL` 설정. 자세한 값은 `apps-script/join/README.md` '현재 배포'.
  - 실연동 확인: wyea.info 페이지에서 fetch로 실제 신청 1건 제출 → `{ok:true}`, 시트에 행 기록 확인, 같은 번호 재제출 → `duplicate`. 시험 행은 삭제함. 접수 서버 주소는 Claude 작업 환경·로컬 VM에서 네트워크 차단이라 브라우저에서만 확인 가능.
  - 홈 '가입하러 가기' 버튼을 `/join`으로 연결(`HomeView.vue`, `useRouter`).
  - 개인정보 처리방침(`PersonalInformationProcessingPolicy.vue`) 1~4항을 가입 동의문과 같게 개정(수집 항목·목적, 탈퇴 시까지 보유, 행정기관 제3자 제공, 초상권 활용·얼굴 가림 요청). 시행일 2026-09-26, 이전 시행일 병기. 5~8항은 그대로.
  - 검증: vue-tsc·eslint(관련 파일)·`npm run build`·모의 테스트 20개 통과, preview에서 홈 버튼 → /join 이동과 관심 활동 14개 표시 확인. GitHub Pages 배포는 아직(대표가 main에 병합하면 자동).
- 2026-09-26 조직도 확정 반영(/join 트랙별 활동 분야 분리): 총회–감사–운영위원회–대표–부대표–사무국–4부(총무·기획·홍보·조직)–대학 지부(조직부 산하), 회원 참여 단위 참여회원/기록단/행사지원단/통번역단/정책제안단/소모임.
  - 일반 트랙: 참여 단위 1개 필수(참여회원이 기본 표시), 통번역단 → '가능 언어' 입력란, 소모임 희망 → '관심 주제' 입력란(둘 다 해당 단위일 때만 필수·저장). 실무진 트랙: 부서 1지망 필수 + 2지망 선택(1지망과 다른 부서만, select에서 1지망은 비활성). 트랙을 바꾸면 활동 분야 선택은 초기화(관심 활동·나머지 입력은 유지).
  - 관심 활동 14개는 그대로 두고 `join.ts`에 `interestMapping`(관심 활동 → 부서·단)과 `recommendDepartments()` 추가. 실무진 트랙에서 1지망을 고르기 전에 관심 활동 기준 추천 부서를 노란 안내로 표시(강제 아님). 관심 활동은 이제 실무진 트랙에서도 1개 이상 필수(추천의 근거라 필요).
  - 시트: 기존 29열 순서 그대로 두고 끝에 `team_second`·`languages`·`club_topic`·`recommended_dept` 4열 추가(총 33열). `team`은 일반=참여 단위, 실무진=부서 1지망. `recommended_dept`는 두 트랙 모두 계산해 저장. `setupSheet`를 다시 실행해 헤더 갱신(데이터 없을 때만 가능).
  - 알림 메일 제목: `[WYEA] 새 회원 가입 신청: 이름 (일반 · 기록단)` / `(실무진 · 홍보부)`. 본문에 2지망·가능 언어·관심 주제·추천 부서 포함.
  - 동의문·개인정보 처리방침 수집 항목에 '활동 분야(참여 단위 또는 희망 부서 1·2지망), 가능 언어(통번역단 선택 시), 관심 주제(소모임 희망 시)'를 추가해 폼과 일치시켰다. 동의 버전은 접수된 신청이 없어 2026-09-25 유지.
  - Apps Script 웹 앱 버전 2 배포(주소 동일). wyea.info에서 일반(통번역단)·실무진(홍보부, 2지망 기획부) 각 1건 실제 접수 → 시트 33열에 정상 기록(가능 언어·2지망·추천 부서 확인) → 시험 행 2개 삭제. 모의 테스트 42개 통과(일반 8·실무진 7 케이스 포함), vue-tsc·eslint·build 통과, preview에서 두 트랙 화면·조건부 입력란·추천 표시 확인.
  - `AboutView.vue` 연혁의 '2025년 7월 14일 재일본대한민국청년회와 협력 관계 체결'은 대표 확인(실제 2025년 6월)과 여전히 다름. 미수정.
- 2026-09-26 조직도 재개편 반영(A 작업): '조직부'→'회원부', '실무진'→'집행부'(트랙 이름·안내 문구·메일 제목·주석; `?track=staff` 파라미터는 유지). 일반 트랙은 '참여회원(기본)'을 없애고 5개 단(기록단·행사지원단·통번역단·정책제안단·소모임) 중 1개 필수 — 회원은 반드시 1개 단에 속함. '소모임 희망'→'소모임'. 소속 단 아래 안내문 "회원은 1개 단에 속하며, 활동 후 참가 기록서를 제출합니다." 추가. `interestMapping`에서 '해외 청년 교류회 참가'는 매핑 제거(전 회원 공통), 조직부 키는 회원부로. 동의문·처리방침의 '실무진 지원 시'→'집행부 지원 시', '참여 단위'→'소속 단', '소모임 희망 시'→'소모임 선택 시'로 표기만 맞춤(동의 버전 2026-09-25 유지, 접수된 신청 없음). 모의 테스트 50개 통과(참여회원·조직부·소모임 희망은 거절되는 케이스 포함). Apps Script 웹 앱 버전 3 배포(주소 동일). wyea.info에서 일반(기록단)·집행부(회원부 1지망, 2지망 총무부) 각 1건 실제 접수 → 시트 정상 기록 확인 → 시험 행 삭제. 옛 이름(참여회원·조직부)으로 보낸 신청은 서버가 거절함을 확인. 시트 헤더는 변경 없음(33열). 커밋·푸시·main 병합·GitHub Pages 배포는 아직.

- 2026-09-23 온보딩 입력 안내 보완: public/onboarding의 HTML·CSS·JS에 이름/아이디/이메일/글자 수 안내, maxlength, 긴 답변과 기타의 실시간 글자 수, 항목별 오류 및 첫 오류 입력창 포커스를 추가했다. 제출 전 서버와 동일한 길이·문자·이메일·관심 분야·동의 규칙을 검사하며 ‘아직 정하지 못함’과 다른 분야 선택은 상호 해제한다. 기존 서버/API/CORS/Sheets 및 사용자 financialReports.ts 변경은 유지했다. JS 구문 검사, 기존 서버 모의 테스트 19개, 프론트/서버 검증 비교 36개 통과. 브라우저 시각/실연동/배포 검증은 이번에 수행하지 않았다.

- 2026-09-22 사용자 요청으로 재정보고 수입·지출 표 합계 행의 100.0% 표시를 제거했다. 표 열 정렬을 위한 빈 셀은 유지하며 개별 항목 비율과 합계 금액은 그대로다. 단순 표시 수정으로 별도 빌드/배포 확인은 하지 않았다.

- 2026-09-22 재정보고 연도 선택 통합: 사용자 요청으로 수입/지출의 독립 선택 대신 단일 activeYear를 사용한다. 어느 쪽 연도 버튼을 눌러도 양쪽 표·차트·선택 표시·잔액 기준이 동시에 전환된다. 기본 2026년은 유지. 앞선 독립 조회 유지 기록보다 이 변경을 우선한다. 모든 버튼과 조회의 공통 상태 참조 확인 및 vue-tsc --noEmit 통과. 브라우저/배포 확인은 하지 않았다.

- 2026-09-22 재정보고 연도 혼합 계산 수정: 기존 remaining은 독립 선택된 수입/지출 합계를 빼서 서로 다른 연도가 섞였다. 이제 지출 탭 선택 연도의 수입 합계에서 같은 연도 지출을 빼며 행 이름도 ‘YYYY년 수입 − 지출’로 명시한다. 수입/지출 탭의 독립 조회는 유지한다. 실제 SFC script를 TypeScript 변환해 Vue computed/ref로 4가지 연도 조합 검증(2025 -874,955원, 2026 -330,000원) 및 vue-tsc --noEmit 통과. 이월금 정책은 추가하지 않았다. 브라우저/배포 확인은 하지 않았다.

- 2026-09-22 재정보고 자동 색상: financialReports.ts에 수입 파랑/지출 초록 8색 팔레트와 withItemColors를 추가했다. 순서대로 배정하고 8개를 넘으면 반복하며 명시적 color는 우선한다. 기존 항목의 색 지정은 제거해 자동 배정을 적용하되 이름/금액(사용자가 추가한 2026 비상주 사무실 계약 330,000원 포함)은 보존했다. FinancialReport.vue의 선택 연도 데이터에서 색을 적용해 표·차트·범례가 동일한 색을 사용한다. vue-tsc --noEmit 통과, 브라우저/배포 확인은 하지 않았다.

- 2026-09-22 재정보고 데이터 분리: src/content/financialReports.ts에 Item 타입과 2025/2026 수입·지출 배열 및 색상 참고 주석을 옮겼다. FinancialReport.vue는 이 데이터를 import하며 연도 선택·표·차트·합계/비율/잔액 계산은 유지한다. 항목명·금액·색상은 변경하지 않았다. 항목 추가는 새 데이터 파일의 해당 연도 배열에서 진행한다. vue-tsc --noEmit 통과. 브라우저/배포 확인은 하지 않았다.

- 2026-09-22 사용자 요청으로 단체소개 주요 활동의 ‘봉사활동 기록 보기 →’ 링크와 더 이상 사용하지 않는 program-link 스타일을 제거했다. 주요 활동 설명은 유지하며 두 활동 링크 모두 표시하지 않는다. 작은 템플릿/CSS 삭제로 별도 빌드·배포 확인은 하지 않았다.

- 2026-09-22 사용자 요청으로 단체소개 주요 활동의 ‘관련 활동소식 보기 →’ 링크만 제거했다. 국제 교류 설명과 봉사활동 기록 링크는 유지했다. 템플릿 한 줄 삭제로 별도 빌드/배포 확인은 하지 않았다.

- 2026-09-22 주요 활동 설명 보강: 사용자 승인에 따라 AboutView.vue의 설립 배경과 연혁 사이에 ‘03 · 주요 활동 / 우리가 하는 일’을 추가했다. 확인된 국제 청년 교류와 지역사회 봉사·협력 설명 및 활동소식/삼원 봉사 상세 링크를 제공한다. 기존 파란 강조색·카드 디자인 유지, 연혁/기본정보 번호는 04/05로 조정. 홈의 활동 목록과 운영 여부 미확정 사업은 변경하지 않았다. 사용자의 소재지 수정 및 기존 미커밋 변경을 보존했다. npm run build 통과 및 정적 페이지 생성 확인, 기존 HomeViewPhoto 중복 import 경고 유지. 브라우저 시각 확인/배포는 하지 않았다.

- 2026-09-22 활동소식 사진 반영: src/content/activities.ts에서 서울 교류회 photos를 251102서울교류회1.jpg → 251102서울교류회2.jpg 순서로 두 장 연결했다. 도쿄 피크닉은 260418도쿄피크닉.jpg, 오사카 교류회는 260421오사카교류회.jpg로 임시 서울 사진을 교체했다. 본문/홈/사용자 AboutView 수정은 보존했다. npm run build 종료 코드 0 및 상세 정적 HTML 생성 확인. 기존 HomeViewPhoto 중복 import 경고 유지, 브라우저 시각 확인/배포는 하지 않았다.

- 2026-09-18 온보딩 폰트: 기존 src/assets/fonts/PretendardVariable.woff2를 public/onboarding에 복사하고 onboarding.css의 @font-face 및 기본 글꼴에 연결했다. 기존 font: inherit 규칙으로 입력창/버튼에도 적용된다. 독립 페이지 배포 경로에 맞춘 상대 URL을 사용하며 폼/API 동작은 변경하지 않았다. 리소스 경로와 상속 규칙 확인, 브라우저 시각 확인은 하지 않았다.

- 2026-09-18 사용자 배포 준비 요청: 온보딩 화면의 테스트/가상 정보 안내 배너를 제거하고 동의 제목·체크박스 및 API 미설정 문구에서 테스트 표현을 제거했다. 동의 본문은 이미 확인된 수집 항목·계정 발급/업무 배치 목적·Sheets 저장·대표 확인 내용으로 변경했다. 보유 기간이나 미확정 운영 정책은 임의로 추가하지 않았다. 최종 동의문과 보유 기간 확정은 여전히 남아 있으며 문구 제거를 운영 정책 확정으로 취급하지 않는다. 과거 테스트 결과/개발 문서의 테스트 표현은 이력으로 보존했다. 기능/API/CSS는 변경하지 않았고 푸시는 사용자 진행. 화면 문구 검색 및 JS 구문 검사 수행, 브라우저·배포 확인은 하지 않았다.

- 2026-09-18 온보딩 탭 표시: 메인 index.html의 favicon은 src/assets/image/wyea-title-logo.png이며 온보딩에는 icon 링크가 없었다. public의 독립 HTML은 Vite 자산 변환 대상이 아니므로 기존 PNG를 public/onboarding/favicon.png로 원본 그대로 복사하고 상대 경로로 연결했다. SHA256 일치 확인. title은 `WYEA | 실무자 온보딩`으로 변경했다. API/폼/CSS는 수정하지 않았고 빌드·브라우저 확인은 별도로 수행하지 않았다.

- 2026-09-18 Sheets 메뉴 구현: Code.gs에 onOpen()과 generateInviteCodeFromMenu()를 추가했다. WYEA 온보딩 → 초대코드 발급에서 기존 생성/중복 검사/잠금 함수를 그대로 호출하고, 반환 코드로 정확한 셀을 찾아 선택한 뒤 완료 안내한다. 설정된 시트 ID와 현재 문서가 다르면 발급하지 않는다. UI 선택 실패와 발급 실패를 구분해 중복 재발급을 유도하지 않는다. verify/submit/CORS/프론트/시트 구조는 변경하지 않았다. 기존 15개와 메뉴 관련 4개 총 19개 모의 테스트 통과. 실제 Sheets 메뉴 UI는 사용자 확인 대기. 기존 프로젝트에 코드 저장 후 시트 새로고침으로 적용하며 메뉴 추가만으로 웹 앱 재배포는 필요하지 않다. 과거 메뉴 미구현 기록보다 이 상태를 우선한다.

- 2026-09-18 온보딩 최신 상태(사용자 실연동 보고): Spreadsheet `onboarding`의 invites/responses 구성, ONBOARDING_SPREADSHEET_ID 설정, Apps Script 웹 앱 배포 및 프론트 API_URL 연결 완료. generateInviteCode 실제 실행·invites 기록, 로컬 /onboarding/index.html 표시, 실제 코드 검증, 가상 정보 제출·responses 저장·used 처리·동일 코드 재접근 차단·새 코드 추가 제출까지 사용자 확인 완료. Codex가 원격 재검증한 결과와 구분한다. 아래 과거 기록의 '설정 대기/실연동 미검증'보다 이 상태를 우선한다. 모바일·폐기 코드·동시 제출·상세 검증·통신 오류·운영 도메인 테스트와 최종 URL/개인정보 정책 확정은 남아 있다.
- 이번 문서 현행화: requirements에 확정된 서버 검증 및 내부 코드 발급 함수, 실제 테스트 상태를 반영하고 README를 갱신했다. Google Sheets의 `WYEA 온보딩 → 초대코드 발급`은 설계만 제안하며 미구현이다. 같은 스프레드시트에 바인딩된 기존 Apps Script에서 onOpen(메뉴 생성)과 메뉴 콜백(기존 발급 함수 호출/결과 안내)을 추가하는 방향이다. Code.gs·프론트엔드·기존 사이트 코드는 변경하지 않았으며 문서 변경만 수행해 테스트는 재실행하지 않았다.

- 2026-09-18 온보딩 서버 보완: 사용자 최신 메시지의 확정 규칙을 적용해 `Code.gs`에 편집기 전용 `generateInviteCode()`와 validate_ 길이/문자/관심 분야 배타 선택 검증을 추가했다. 현재 디스크의 requirements에는 이 상세 규칙·발급 함수가 아직 미반영되어 있음을 알렸으며, 이번 요청을 우선했다. 발급 함수는 UUID 기반 32자 코드, 발급 시각, active, 빈 used_at/response_id를 한 행 기록하고 잠금 및 기존 invites/responses 코드 충돌 검사를 수행한다. doGet/doPost는 발급을 호출하지 않는다. 테스트와 README를 갱신했다. 기존 8개 포함 총 15개 서버 모의 테스트 통과. 실제 Apps Script/Sheets 실행·배포는 미검증이며 새 Code.gs를 편집기에 반영하고 기존 웹 앱 배포 버전을 갱신해야 서버 검증이 적용된다. 프론트엔드 및 기존 사이트 코드는 수정하지 않았다. 아래의 ‘발급 도구 미포함’ 기록은 이전 구현 이력이다.

- 2026-09-18 온보딩 1차 구현: `onboarding-requirements.md`를 유일한 요구사항 기준으로 사용했다(사용자 메시지의 `nboarding-requirements.md`는 실제 파일명과 다름). `public/onboarding/`에 독립 정적 HTML/CSS/JS 및 기존 로고 사본, `apps-script/onboarding/`에 Code.gs·설정/배포/실연동 테스트 README·서버 모의 테스트를 추가했다. 기존 Vue 라우터·공통 레이아웃·배포 설정과 사용자의 AboutView/개인정보처리방침 변경은 수정하지 않았다.
- 온보딩 구현 범위: 개인별 코드 verify → 필수 계정/활동/관심 정보와 테스트 동의 → submit → Sheets 한 행 기록 및 코드 used 처리 → 완료 화면. 서버 필수값 검증, 스크립트 잠금, 기존 응답 재확인, 수식 입력 이스케이프를 적용했다. 저장 후 used 갱신만 실패한 경우 재시도에서 중복 행을 생성하지 않고 상태를 정리한다. API POST 본문은 `{action:'submit', code, payload:{...}}`, Content-Type은 `text/plain;charset=utf-8`이다. 이메일·사진 업로드·자동 계정 생성·코드 발급 도구·별도 서버/DB는 추가하지 않았다.
- 온보딩 설정 대기: Apps Script 스크립트 속성 `ONBOARDING_SPREADSHEET_ID`에 사용자 생성 테스트 시트 ID를 설정하고 웹 앱으로 배포한 뒤, `public/onboarding/onboarding.js`의 빈 `API_URL`에 `/exec` 주소를 넣어야 한다. 설정 전에는 코드 확인 버튼을 비활성화한다. `/onboarding/`은 개발 예시 경로이며 최종 URL, 실제 개인정보 동의문/보유 기간, 최종 관심 분야는 미확정이다. 현재는 가상 데이터만 사용한다.
- 이번 온보딩 검증: 서버 모의 테스트 8개, 프론트엔드 JS 구문 검사, 신규 JS/테스트 파일의 ESLint 검사 통과. `npm run build` 종료 코드 0, 기존 정적 페이지 생성 및 `dist/onboarding` 4개 파일 포함 확인. 기존 HomeViewPhoto 정적/동적 중복 import 경고와 프리렌더 종료 시 `The build was canceled` 로그가 남았다. 실제 Apps Script/Sheets 연결, 브라우저 CORS 응답, 모바일 화면·제출, GitHub Pages 배포는 아직 검증하지 않았다. 실연동 테스트를 마치기 전 §18 전체 완료로 기록하지 않는다. 커밋·푸시·개발 서버 실행은 하지 않았다.

- 2026-09-18: 사용자 요청으로 단체소개 하단의 ‘이메일로 문의하기’와 ‘재정보고 보기 →’ 링크 및 전용 스타일을 제거했다. 문의 안내 문구와 단체 기본정보의 연락처는 유지한다. 작은 템플릿/CSS 삭제로 별도 빌드·배포 확인은 하지 않았다.

- 2026-09-17: 모든 활동 데이터의 필드를 `path → title → date → startDate → endDate → location → category → partnerLabel → partner → paragraphs → photos` 순서로 통일했다. 국제교류에도 partnerLabel을 명시하고, 단일 photo는 제거하여 사진 한 장도 photos 배열로 관리한다(사진 없음은 빈 배열). paragraphs도 모두 여러 줄 형식으로 통일했다. 기존 본문·사진·목록 순서는 유지했고 타입 검사 통과. 앞으로 photo/heading/summary를 사용한다는 이전 지침 대신 이 구조를 따른다. 배포/브라우저 확인은 하지 않았다.

- 2026-09-17: 국제교류 4건에 EXCHANGE가 이미 지정돼 있음을 재확인했다. 사용자가 코드에서 쉽게 찾도록 모든 활동의 category 위치를 봉사활동과 동일하게 location 바로 아래로 통일했다. 값과 화면 동작은 그대로이며 별도 빌드는 하지 않았다.

- 2026-09-17: 국제교류 4건에 `category: 'EXCHANGE'`를 명시했다. `activityCategories`에 EXCHANGE=국제교류, VOLUNTEERING=봉사활동을 정의하고 category를 필수 타입으로 지정했다. 목록 배지는 이 매핑을 사용하고 상세는 명시된 category를 표시한다. 기존 사용자 작성 봉사활동 본문은 보존했다. 타입 검사 수행, 배포/시각 확인은 하지 않았다.

- 2026-09-17: 사용자 요청으로 `summary`를 제거하고 모든 활동 본문을 필수 `paragraphs` 배열로 통일했다. 서울 원문 3문단은 유지하고 나머지 기존 짧은 설명은 한 문단으로 옮겼다. 라우터·프리렌더 메타 설명은 날짜와 제목으로 생성한다. 앞으로 본문 수정은 paragraphs만 편집한다. 타입 검사 통과, 빌드 검증 수행. 배포/브라우저 확인은 하지 않았다.

- 2026-09-17: 사용자 요청으로 단체소개 연혁에서 서울 교류회(2025-11-01~03) 항목을 제거하고, ‘2026년 3월 4일 삼원가정봉사원파견센터와 협력 관계 체결’을 추가했다. AboutView의 서울 활동 데이터 import도 제거했다. 활동소식의 서울 교류회와 봉사활동 기록은 그대로 유지한다. 단순 연혁 데이터 변경으로 별도 빌드/배포 확인은 하지 않았다.

- 2026-09-17: 사용자 요청으로 모든 활동 데이터의 `heading`과 상세 본문 소제목 표시를 제거했다. 상세 카드에는 사진 → 본문 → 일정·기관 정보 순서로 표시한다. 페이지 상단 행사 제목은 유지한다. 작은 데이터/템플릿 삭제로 별도 빌드·배포 확인은 하지 않았다.

- 2026-09-17: 서울 교류회에 사용자가 제공한 원문 3문단을 `paragraphs` 배열로 추가했다. 상세 화면은 사진 다음에 소제목과 본문 3문단을 표시하며, 다른 활동은 기존 summary를 표시한다. 서울 교류회의 짧은 summary는 메타 설명용으로 유지한다. 원문은 임의로 변경하지 않았다. `npm run build` 검증 수행, 배포/브라우저 시각 확인은 하지 않았다.

- 2026-09-17: 사용자 요청으로 활동 상세 카드 내부 순서를 사진 → 소제목·설명 → 일정·기관 정보로 변경했다. 페이지 상단의 행사 제목과 날짜는 유지한다. 템플릿 순서만 수정했으며 별도 빌드/배포 확인은 하지 않았다.

- 2026-09-17: 사용자 요청으로 활동소식 목록과 상세 상단의 날짜 옆 ‘· 장소’ 표시를 모두 제거했다. 날짜만 표시하며 상세 정보 표의 ‘지역’과 원본 location 데이터는 유지한다. 템플릿 두 곳만 수정했으며 별도 빌드/배포 검증은 하지 않았다.

- 2026-09-17: 음악교류회 상세에 사용자가 지정한 `260829제2회한일음악교류회1.jpg`, `260829제2회한일음악교류회2.jpg`를 연결했다. 활동 데이터의 선택 항목 `photos` 배열로 여러 사진을 세로로 표시하며 원본 비율을 유지한다. 기존 단일 `photo`도 계속 지원한다. 홈 사진 경로는 사용자가 이미 새 이름으로 수정한 상태라 유지했다. `npm run build` 통과, 브라우저 시각 확인/배포는 하지 않았다. 음악교류회 사진은 더 이상 대기 사항이 아니며 장소·상대 단체는 미확정이다.

- 2026-09-17: ‘제2회 한일음악교류회’(2026-08-29)를 `/activities/2026-korea-japan-music-exchange`에 추가했다. 활동소식은 현재 총 5건이며 최신순으로 표시한다. 장소·상대 단체·사진은 추정하지 않고 생략했다. 해당 데이터가 없는 경우 상세 정보 행과 날짜 옆 구분점도 표시하지 않는다. 빌드 및 정적 상세 HTML 생성 성공, 실제 배포/브라우저 확인은 하지 않았다.

- 2026-09-17: 사용자가 추가한 봉사활동 사진 `260304삼원가정봉사원파견센터봉사활동.jpg`를 `samwonPhoto`로 import하여 연결했다. `photo`에 `@/assets/...` 문자열을 직접 쓰면 브라우저 경로로 변환되지 않으므로 import 변수를 사용한다. `npm run build` 통과. 실제 브라우저 시각 확인은 하지 않았다. summary 삭제는 중단 후 작성 방법 논의로 바뀌었으므로 기존 summary를 유지 중이다.

- 2026-09-17 사진 파일명 변경 반영: `LINE_ALBUM_2日目_251209_16.jpg` → `251102서울교류회1.jpg`, `KakaoTalk_20251210_171433166.jpg` → `251102서울교류회2.jpg`. `HomeViewPhoto.vue`와 `src/content/activities.ts`의 참조를 수정했다. 위의 이전 파일명 기록은 과거 이력이며 앞으로 새 파일명을 사용한다. `npm run build` 통과, 이미지 경로 관련 경고 없음(기존 HomeViewPhoto 중복 import 경고는 유지). 배포/브라우저 확인은 하지 않았다.

- 사용자 요청으로 단체소개와 활동소식(목록/상세)의 강조색을 파란 계열로 통일했다. `AboutView.vue`, `ActivitiesView.vue`의 라벨·링크·연혁·버튼은 `#0d47a1`, 구분선은 연한 파랑, 보조 정보는 청회색을 사용한다. 기존 배치와 홈 커팅매트는 유지한다. 색상 값만 수정했으며 별도 빌드/브라우저 검증은 하지 않았다.

- 2026-09-17: `activities` 배열 기반으로 목록·상세 조회·라우터·프리렌더를 통합했다. 활동 추가 시 `src/content/activities.ts`에 데이터를 정의하고 배열에 포함한다. 각 활동의 `heading`, `photo`도 이 파일에서 수정한다.
- 사용자 요청으로 도쿄 피크닉(재일본대한민국청년회, 2026-04-18)과 오사카 교류회(재일본대한민국청년회 오사카지부, 2026-04-21)를 추가했다. 설명은 확인된 일시·장소·단체만 담은 짧은 초안이며, 두 사진은 기존 `LINE_ALBUM_2日目_251209_16.jpg`를 임시 사용한다. 추후 설명과 사진 교체 예정. 단체소개 연혁에는 이번 두 활동을 추가하지 않았다.
- 이번 변경은 `npm run build`, `npx vue-tsc --noEmit` 통과. 생성 HTML에서 세 상세 페이지의 서로 다른 제목을 확인했다. 새 상세 페이지에도 기존 진입 애니메이션을 적용했다. 배포/브라우저 확인은 수행하지 않았다.
- 추가 활동을 기다리는 동안 주요사업 설명, 참여·문의 안내 등 초기 계획의 미완료 콘텐츠도 남아 있다. 사진 연결만 완료했다고 재심사 준비가 모두 끝났다고 단정하지 않는다.
- Ad Grants 정책을 다시 평가할 때는 Google 공식 최신 정책을 확인한다. 문서의 계획이나 로컬 빌드 성공은 승인 보장이 아니다.

- 2026-09-22: 사용자 요청으로 단체소개 기본정보의 소재지를 `경남 창원시 성산구 용지로169번길 11-31`로 변경했다. 표시 문구 한 곳만 수정했으며 별도 빌드·배포·브라우저 확인은 하지 않았다.

## 2026-09-26 D 푸터·홈 사진 슬라이드쇼

- `feature/footer`는 A 배포 기록이 반영된 main `858e718`에서 분기했다. `git fetch` 시 origin/main에 별도의 9/25 푸터 커밋은 없었다. 최신 사용자 지시에 따라 계속 진행했다.
- `SiteFooter.vue`로 홈과 나머지 페이지의 푸터를 공통화했다. PC 3열·태블릿 2열·모바일 1열, 대표·주소·고유번호·이메일·카카오 채널 준비 중·인스타그램·처리방침을 표시한다. 기존 출처 표기 의무가 있던 래스터 아이콘, X 링크 및 사용하지 않는 푸터 스타일을 제거했다. 참가 기록서 링크는 B 완료 후 연결한다.
- `HomeViewPhoto.vue`의 활동 사진에 이전/다음 버튼, 사진 클릭 시 `/activities` 이동, pointer 스와이프를 추가했다. 이미지 드래그와 텍스트 선택을 막고 클릭과 드래그를 구분한다.
- 검증: `npx vue-tsc --noEmit`, 변경 파일 ESLint, `npm run build` 통과. 사용자 지시에 따라 브라우저를 사용하지 않아 PC·모바일 시각 확인, 스크린샷, 실제 드래그/클릭 동작은 미확인이다. 빌드의 기존 HomeViewPhoto 정적/동적 import 경고는 남아 있다.

### D 모바일 제보 후 수정 (2026-09-26)
- 대표가 비공개 이슈 #6에서 모바일 홈 푸터가 사진을 가리고, 활동 사진 배경/제목이 보이지 않으며, 사진 전환 때 두 장이 보이는 현상을 제보했다. 모바일 고정 헤더도 스크롤을 따라 내려온다고 지적했다.
- 홈 푸터를 고정 오버레이에서 본문 마지막의 일반 흐름으로 변경해 사진을 덮지 않게 했다. 홈 모바일에서 헤더는 첫 화면에만 두고, 사진 배경은 앞쪽 스택에 놓되 앞의 두 섹션보다 아래에 둔다. 슬라이드에서 다음 사진을 미리 노출하지 않고 전환 타이머를 정리한다.
- 검증: vue-tsc·ESLint·빌드 통과. 대표가 제보한 모바일 시각 결과는 재배포 후 대표 확인 대기.

## 2026-09-26 B+C 운영 스크립트 진행 기록

- `feature/report`는 D 병합 main에서 분기했다. `apps-script/ops` standalone 프로젝트(`1Cj7qxoUcl2pL2-M5MGO3-9hjyxj2nRyJclZPQRlb-IcI5JcwYrmfJRYq`)를 clasp로 만들었다. 대표가 wyea@wyea.info 계정으로 `setupAll` 첫 권한을 승인했다. 첫 실행은 기본 Cloud 프로젝트의 Sheets API 비활성화로 중단됐고, manifest에 고급 Sheets 서비스를 선언하여 해결했다. 다음 실행은 새 폼 항목 변환 방식 오류로 중단됐고, 코드 수정 후 승인된 웹 앱을 통해 재실행했다.
- 공유 드라이브 WYEA_LCE에 `WYEA 참가 기록서` 시트(`1HBM6gHKqnhn_kSiZbf-7cKPJXd3J7D-YeM37Fr0eOF4`)가 만들어지고 이동 성공 로그가 있었다. `records`·`events`(첫 행사 `26-2기 회원 OT`) 탭과 부서 필터 보기 4개 생성 로그가 확인됐다. `GET ?action=events`는 `{ok:true,events:['26-2기 회원 OT']}`를 반환했다.
- 웹 앱 배포 ID `AKfycbxqAfHZmTqKh0ZVTW07BXDIJWawe4qMG1-Avcdy2tgTSve0tA9EsFmgtAtBMJenPDVv`, 현재 v9. `/report` 프런트 주소는 `src/content/report.ts` 참조. 관리자 시험/확인 경로는 레포에 placeholder만 있고 실제 64자 비밀값은 레포 밖 `.ops.env`에 저장했다. 임시 배포 사본에서만 치환한다.
- C: 기존 회원 정보 갱신 폼의 실제 `getItems` 목록에서 소속 단(객관식)·통번역단/소모임(섹션)·가능 언어/관심 주제(단답) 확인. 단 5개 선택지 모두 섹션 이동, 기존 필수 동의 체크박스의 세 선택지와 `/join`의 세 동의 상세 문구 도움말 확인. 정확히 3개 선택을 요구하도록 검증을 추가했다. 처리방침 URL·동의 버전 문구 확인. 응답 시트에 소속 단·가능 언어·관심 주제·비고 열이 있고 기존 응답 2행 모두 비고 `재제출 대상`으로 확인. 중복 생성된 동의 문항과 빈 열은 정리했으며 응답 행은 삭제하지 않았다. 옛 26-2기 가입 폼은 응답 중지·설명 한 줄 변경 확인, 기존 응답 0행.
- B 프런트/서버: 이름+휴대전화 둘 다 일치해야 대학·단만 조회; 행사 목록, 4섹션 서버 검증, 부서별 열 접두어, 메일 알림, 홈/푸터 링크와 프리렌더 구현. 모의 테스트는 15개 통과. 실제 공개 `/report` 배포·제출·시험 행 삭제는 다음 단계에서 확인해야 한다.
- 처리방침 제안(대표 확인 필요): 참가 기록서의 수집 항목에 `행사명·일시·역할·참가 확인, 후기·사진 링크, 의견·제안, 소속·재참여 의사`와 수집 목적(활동 기록·보고·다음 행사 기획)을 명시한다. 사진 링크 입력란에는 `타인이 식별되는 사진은 당사자 동의 후 제출` 안내를 둔다. 기존 가입 초상권 동의가 타인 사진 제출까지 포괄하는지는 확정하지 않았다.

### D 모바일 푸터 추가 수정 (2026-09-26)
- 대표의 이슈 #6 후속 지시에 따라 홈 사진이 푸터 위에 겹쳐 보이지 않도록 푸터의 쌓임 순서를 올렸다. 주소와 고유번호를 두 줄로 분리하고, 인스타그램 텍스트를 `IG @wyea_official`로 바꿨다.
- vue-tsc·ESLint·빌드 통과. 모바일 실제 화면은 대표 재확인 대기.
