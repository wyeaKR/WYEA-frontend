# WYEA 실무자 온보딩 기능 요구사항

> 이 문서는 현재 합의된 **최신 요구사항**을 정리한 기준 문서다.  
> 기존 `WYEA_온보딩_개발_가이드라인.md`와 내용이 충돌할 경우 **이 문서를 우선한다**.  
> 기존 가이드의 Apps Script/CORS/보안 관련 내용은 참고할 수 있으나, 기존 가이드에 있던 모든 기능을 구현 대상으로 간주하지 않는다.

## 1. 목적

세계청년교류연합(WYEA)에 새로 참여하는 **실무자**를 대상으로 온보딩 정보를 수집한다.

이 기능은 일반 회원가입 기능이 아니다.

주요 목적은 다음과 같다.

- `@wyea.info` Google Workspace 계정 생성에 필요한 정보 수집
- 실무자가 단체에서 얻고 싶은 것 파악
- 실무자가 하고 싶은 활동 파악
- 희망 업무 및 관심 분야 파악
- 대표가 이후 계정 발급 및 업무 배치를 판단할 수 있도록 정보 정리

Workspace 계정 자체를 자동 생성하거나 업무를 자동 배정하지 않는다.

## 2. 1차 구현 범위

이번 1차 구현은 아래 흐름까지만 구현한다.

**개인별 1회용 초대코드 입력 → 코드 확인 → 정보 입력 → Google Sheets 자동 저장 → 초대코드 사용 처리 → 완료 화면**

데이터는 실제 운영 시트가 아닌 **테스트용 Google Sheets**를 새로 만들어 사용한다.

기존 회원관리 시트나 다른 운영 명부와의 연동은 이번 범위에서 고려하지 않는다.

## 3. 제외 범위

다음 기능은 이번 구현에 포함하지 않는다.

- 이메일 발송
- 프로필 사진 업로드
- Google Drive 사진 저장
- Workspace 계정 자동 생성
- 관리자 대시보드
- 별도 로그인/회원가입 시스템
- 별도 Node.js 서버
- 외부 데이터베이스
- 기존 가이드의 9개 온보딩 안내 섹션
- 첫 30일 목표 입력
- 여권 보유 여부
- 긴 온보딩 안내 콘텐츠

필요해질 경우 추후 별도 요구사항으로 추가한다.

## 4. 페이지 구조

기존 `wyea.info` 사이트의 Vue/Vite 구조와 정적 배포 방식을 유지한다.

온보딩 기능 때문에 기존 사이트 전체를 동적 서버 구조로 변경하지 않는다.

온보딩 페이지는 기존 Vue 라우터 및 공통 레이아웃과 최대한 분리된 **독립 정적 페이지**로 구성하는 방향을 우선한다.

예시 구조:

```text
public/
  onboarding/
    index.html
    onboarding.css
    onboarding.js
    logo.png

apps-script/
  onboarding/
    Code.gs
    README.md
```

최종 URL 경로는 아직 확정하지 않는다. `/onboarding`은 개발상 예시 경로일 뿐이다.

## 5. 화면 흐름

### 5.1 초대코드 화면

사용자가 개인별 초대코드를 입력한다.

프론트엔드는 Apps Script의 `verify` API를 호출한다.

유효한 코드일 경우 정보 입력 화면으로 이동한다.

유효하지 않거나 이미 사용되었거나 폐기된 코드는 구체적인 내부 상태를 노출하지 않고 동일한 실패 메시지로 처리한다.

### 5.2 정보 입력 화면

코드 확인이 성공한 사용자에게 온보딩 폼을 표시한다.

### 5.3 제출 완료

Apps Script가 서버에서 코드를 다시 확인한 뒤 응답을 테스트용 Google Sheets에 저장한다.

저장이 정상적으로 완료되면 초대코드를 `used` 상태로 변경한다.

사용자는 완료 화면을 확인한다.

## 6. 입력 항목

### 계정 발급 정보

| 항목 | 필드명 | 방식 | 필수 |
|---|---|---|---|
| 한글 성명 | `name_ko` | 텍스트 | 필수 |
| 영문 성 | `last_name_en` | 텍스트 | 필수 |
| 영문 이름 | `first_name_en` | 텍스트 | 필수 |
| 희망 계정 아이디 | `desired_id` | 텍스트 | 필수 |
| 계정 안내받을 이메일 | `contact_email` | 이메일 | 필수 |

희망 계정 아이디는 사용자가 원하는 값을 수집하는 용도이며 실제 사용 가능 여부 및 최종 계정명은 대표가 결정한다.

영문 성명은 여권 정보 수집 목적이 아니라 Workspace 계정 표시 이름 등에 활용할 정보로 취급한다.

### 활동 및 업무 관련 정보

| 항목 | 필드명 | 방식 | 필수 |
|---|---|---|---|
| 단체에서 얻고 싶은 것 | `expectations` | 여러 줄 텍스트 | 필수 |
| 하고 싶은 활동 | `desired_activities` | 여러 줄 텍스트 | 필수 |
| 희망 업무·관심 분야 | `interests` | 복수 선택 | 필수 |
| 기타 관심 분야 | `interest_other` | 텍스트 | 기타 선택 시 필수 |

관심 분야의 초기 선택지는 다음과 같이 한다.

- 행정·문서
- SNS·콘텐츠
- 행사 기획
- 대외협력
- 지부 운영·모집
- 디자인
- 번역
- 사진·영상
- 웹·개발
- 아직 정하지 못함
- 기타

선택지는 추후 실제 운영에 맞게 변경할 수 있다.

### 확정된 서버 검증 규칙

문자열은 양끝 공백 제거 후 검사한다. 프론트엔드 검증과 별도로 Apps Script에서 반드시 검증한다.

- `name_ko`: 필수, 최대 30자
- `last_name_en`, `first_name_en`: 필수, 각각 최대 50자, 영문자/공백/하이픈/ASCII apostrophe만 허용
- `desired_id`: `[a-z0-9.]`만 허용, 3~30자
- `contact_email`: 필수, 이메일 형식
- `expectations`, `desired_activities`: 필수, 각각 최대 1000자
- `interests`: 허용된 선택지에서 최소 1개. `아직 정하지 못함`은 다른 분야와 동시 선택 불가
- `interest_other`: 기타 선택 시 필수, 최대 200자
- `agree_privacy`: boolean `true` 필수

### 개인정보 동의

`agree_privacy` 필드를 둔다.

개인정보 수집·이용 동의는 필수로 한다.

실제 운영 전에는 최종 개인정보 동의문과 보유 기간을 별도로 확정해야 한다. 테스트 단계에서는 실제 개인정보 대신 가상 데이터를 사용한다.

## 7. 초대코드

초대코드는 **개인별 1회용**으로 사용한다.

초대코드는 다음 역할만 담당한다.

- 온보딩 폼 진행 권한 확인
- 허가되지 않은 사용자의 제출 방지
- 한 초대코드당 하나의 응답만 허용

초대코드는 본인 인증 수단이 아니다.

코드를 다른 사람에게 전달하지 않았다는 사실까지 검증하는 기능은 이번 범위에 포함하지 않는다.

정적 HTML/JS 자체를 비공개로 만드는 수단으로도 간주하지 않는다.

## 8. `invites` 테스트 시트

초기 테스트에서는 다음 구조를 사용한다.

| 열 | 필드 | 설명 |
|---|---|---|
| A | `code` | 개인별 랜덤 초대코드 |
| B | `issued_at` | 발급 시각 |
| C | `status` | `active` / `used` / `revoked` |
| D | `used_at` | 사용 완료 시각 |
| E | `response_id` | 연결된 제출 기록 식별자 |

필요하면 추후 관리 편의를 위해 대상자 이름이나 만료일 등을 추가할 수 있다.

초대코드는 충분히 긴 랜덤 문자열을 사용한다. 기존 가이드의 16자 이상 난수 권고를 참고할 수 있다.

관리자용 `generateInviteCode()`를 Apps Script 내부 함수로 제공한다. 현재 구현은 UUID 기반 32자 코드의 기존 invites/responses 중복 여부를 확인하고, `invites`에 코드·발급 시각·`active`·빈 used_at·빈 response_id를 한 행 저장한다. 웹 API로 발급 기능을 노출하지 않는다.

## 9. `responses` 테스트 시트

한 번의 제출을 한 행으로 저장한다.

| 열 | 필드 |
|---|---|
| A | `response_id` |
| B | `submitted_at` |
| C | `invite_code` |
| D | `name_ko` |
| E | `last_name_en` |
| F | `first_name_en` |
| G | `desired_id` |
| H | `contact_email` |
| I | `expectations` |
| J | `desired_activities` |
| K | `interests` |
| L | `interest_other` |
| M | `agree_privacy` |

`response_id`와 `submitted_at`은 서버에서 생성한다.

`interests` 복수 선택값은 Sheets에서 읽기 쉬운 문자열 형태로 저장할 수 있다.

사용자 입력값이 Google Sheets 수식으로 의도치 않게 실행되지 않도록 안전하게 저장한다.

## 10. Apps Script 역할

Apps Script는 다음 작업을 담당한다.

- 초대코드 유효성 확인
- 제출 시 초대코드 재검증
- 서버 측 입력값 검증
- 중복 제출 방지
- 테스트용 Google Sheets에 응답 저장
- 사용된 초대코드 상태 변경

브라우저 코드에는 시트 접근 권한이나 초대코드 전체 목록 등 민감한 관리 데이터를 넣지 않는다.

## 11. API 초안

### Verify

```text
GET {API_URL}?action=verify&code={CODE}
```

성공:

```json
{
  "ok": true
}
```

실패:

```json
{
  "ok": false,
  "error": "invalid_code"
}
```

존재하지 않는 코드, 이미 사용된 코드, 폐기된 코드 등은 외부에서 구분할 필요가 없으므로 동일한 `invalid_code`로 처리한다.

### Submit

POST 요청 본문에는 `action`, `code`, 폼 데이터를 전달한다.

성공 예시:

```json
{
  "ok": true,
  "response_id": "..."
}
```

입력값 검증 실패 예시:

```json
{
  "ok": false,
  "error": "validation_failed"
}
```

서버 오류:

```json
{
  "ok": false,
  "error": "server_error"
}
```

## 12. Apps Script 통신 원칙

기존 개발 가이드의 Apps Script/CORS 관련 설계를 참고한다.

POST는 우선 다음 방식으로 구현 및 실제 배포 환경에서 검증한다.

```javascript
await fetch(API_URL, {
  method: 'POST',
  headers: {
    'Content-Type': 'text/plain;charset=utf-8'
  },
  body: JSON.stringify(payload)
});
```

- 불필요한 커스텀 헤더를 추가하지 않는다.
- `application/json`으로 임의 변경하지 않는다.
- 존재하지 않는 `TextOutput.setHeaders()` 등의 API를 사용하지 않는다.
- `no-cors`로 오류를 숨기는 방식은 사용하지 않는다.
- 실제 Apps Script 배포 URL에서 응답 JSON까지 정상적으로 읽을 수 있는지 확인한다.

통신 방식에 문제가 발생하면 검증되지 않은 우회 코드를 추가하기 전에 원인을 확인한다.

## 13. 중복 제출 방지

제출 시 서버에서 다음 순서를 기본으로 한다.

1. 요청 형식 및 입력값 검증
2. `LockService` 등을 이용해 동시 처리 방지
3. 초대코드가 `active`인지 재확인
4. 해당 코드로 이미 저장된 응답이 있는지 확인
5. `responses`에 한 행 저장
6. 초대코드를 `used`로 변경
7. `response_id`와 `used_at` 기록
8. 성공 응답 반환

테스트 단계에서는 과도하게 복잡한 복구 시스템을 만들지 않는다.

다만 네트워크 재시도나 중복 클릭으로 같은 초대코드의 응답이 여러 행 생성되지 않도록 하는 것은 필수다.

## 14. 프론트엔드 요구사항

- 모바일에서도 정상적으로 사용할 수 있어야 한다.
- 제출 중에는 제출 버튼을 비활성화한다.
- 필수값을 사용자에게 명확하게 표시한다.
- 서버에서도 동일한 필수값 검증을 수행한다.
- 오류가 발생했을 때 가능한 한 사용자가 작성한 값을 유지한다.
- 초대코드 확인 전에는 실제 제출 폼을 사용할 수 없게 한다.
- 내부 초대코드 목록이나 관리용 정보는 클라이언트에 포함하지 않는다.

## 15. 검색 노출

온보딩 페이지는 일반 홈페이지 메뉴에서 적극적으로 노출하지 않는 방향으로 한다.

가능하면 다음 메타 태그를 적용한다.

```html
<meta name="robots" content="noindex,nofollow">
```

단, URL 비공개나 `noindex`는 보안 수단이 아니다. 실제 제출 권한은 반드시 서버의 초대코드 검증으로 통제한다.

## 16. 현재 미확정 사항

다음 항목은 구현 또는 실제 운영 전 확정이 필요하다.

- 최종 온보딩 URL 경로
- 최종 개인정보 수집·이용 동의문
- 개인정보 보유 기간
- 관심 업무 분야의 최종 선택지
- Google Sheets 메뉴를 통한 발급 편의 기능은 구현 완료, 실제 메뉴 UI 확인 대기. `onOpen()`이 메뉴를 만들고 `generateInviteCodeFromMenu()`가 기존 발급 함수를 호출해 코드 셀 선택 및 완료 안내를 제공한다. 편집기용 `generateInviteCode()`는 사용자 실테스트 완료

이 항목들은 확정되지 않았다는 이유로 임의의 운영 정책을 만들어 구현하지 않는다.

## 17. Codex 작업 원칙

Codex는 이 문서를 현재 온보딩 기능의 **최우선 요구사항 문서**로 사용한다.

기존 `WYEA_온보딩_개발_가이드라인.md`는 기술적 참고자료로 사용할 수 있지만 이 문서와 충돌하는 경우 이 문서를 따른다.

특히 기존 가이드에 있다는 이유만으로 다음 기능을 임의로 추가하지 않는다.

- 이메일 알림
- 사진 업로드
- 9개 안내 섹션
- 첫 30일 목표
- 여권 관련 문항
- Workspace 계정 자동 생성

요구사항이 모호하거나 이 문서와 현재 코드가 충돌하면 임의로 판단하여 기능을 확장하기보다 먼저 사용자에게 확인한다.

## 18. 1차 완료 기준

다음 조건을 만족하면 1차 테스트 구현을 완료한 것으로 본다.

- 유효한 초대코드로 폼에 진입할 수 있다.
- 잘못된/사용된/폐기된 코드로 진행할 수 없다.
- 필수 정보를 입력해 제출할 수 있다.
- 응답이 테스트용 `responses` 시트에 정확히 한 행 저장된다.
- 제출된 초대코드가 `used` 처리된다.
- 같은 코드로 두 번째 응답을 저장할 수 없다.
- 새 초대코드로 새로운 응답을 제출할 수 있다.
- 모바일 환경에서도 기본적인 입력과 제출이 정상 동작한다.
- 기존 `wyea.info` 사이트의 다른 기능에 영향을 주지 않는다.

## 19. 현재 검증 상태 (2026-09-18 사용자 확인)

- 테스트 Google Spreadsheet `onboarding` 및 `invites`, `responses` 탭 구성 완료
- `ONBOARDING_SPREADSHEET_ID` 설정, Apps Script 웹 앱 배포, 프론트 `API_URL`에 `/exec` 연결 완료
- `generateInviteCode()` 실제 실행 및 생성 코드의 invites 기록 확인
- 로컬 `/onboarding/index.html` 화면 표시 및 실제 코드 검증 성공
- 가상 정보 제출, responses 저장, 초대코드 사용 처리 성공
- 동일 코드 재접근 시 동일 실패 문구로 차단 확인
- 새 코드 생성 후 추가 제출·저장 성공

이는 사용자가 직접 수행해 보고한 실제 연동 결과이며 Codex가 원격으로 재검증한 결과는 아니다. 핵심 정상 흐름은 실연동 완료로 취급한다. 모바일, 폐기/없는 코드, 동시 제출, 입력 경계값 및 통신 오류 시나리오의 실제 환경 검증과 GitHub Pages의 운영 도메인에서의 검증은 별도 확인이 남아 있다. 개인정보 동의문/보유 기간 및 최종 URL도 미확정이므로 §18 전체와 운영 전환 완료를 의미하지 않는다.
